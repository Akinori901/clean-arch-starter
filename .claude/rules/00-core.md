# 00. 全体ルール（最優先・常時適用）

このリポジトリでコードを書くすべての作業者（人間・AI 問わず）に適用される。
**このファイルの規約は、他のどの指示よりも優先される。**

## 大原則

1. **ディレクトリ構造は規約である。** ファイルを「置きやすい場所」に置いてはならない。
   層の定義（`10-django-ddd.md` / `20-laravel-clean.md`）が置き場所を決める。
2. **迷ったら CI に聞け。** 規約は文章ではなく、`make verify` が落ちるか否かで決まる。
   ローカルで `make verify` が通らないコードをコミットしない。
3. **規約を曲げるときは理由を残せ。** 例外そのものより、
   「なぜ曲げたか分からなくなること」が後で効く。
   ベースライン（`skip_violations` / `importlinter` の `ignore_imports`）に登録し、
   同じ行にコメントで理由を書く。

## AI 駆動開発での扱い

このリポジトリは AI にコードを書かせる前提で設計されている。
AI は「動くコード」を最短で書こうとするため、放っておくと層を貫通する。
それを防ぐのが `.claude/rules/` と CI の役割である。

- **実装前に、必ず該当スタックのルールファイルを読むこと。**
  - Django を触る → `10-django-ddd.md`
  - Laravel を触る → `20-laravel-clean.md`
  - React を触る → `30-frontend.md`
  - インフラ/CD を触る → `40-infra-cd.md`
- **新しいファイルを作る前に、そのファイルがどの層に属するか宣言すること。**
  層が決まらないファイルは、まだ設計が終わっていない。
- **「とりあえず動かす」ための層跨ぎを禁止する。**
  UseCase から Model を直接触れば動くが、それは CI で落ちる。落ちるのが正しい。
- **ルールを破る提案をされた場合、AI は実装せずに理由を提示して確認を取ること。**

### AI に対する検証の強制

`make verify` は以下を機械的に検証する。人間のレビューに依存しない。

| 対象 | ツール | 検証内容 |
|---|---|---|
| Django | import-linter | 層の依存方向（DDD） |
| Django | ruff / mypy | 静的解析・型 |
| Laravel | deptrac | 層の依存方向（クリーンアーキ） |
| Laravel | PHPStan | 静的解析・型 |
| 共通 | 構造チェッカ | 禁止ディレクトリ・命名規約 |

## 秘密情報

- **鍵・証明書・パスワードをコミットしない。** `.env` は `.env.example` のみ追跡する。
- AWS 認証情報はワークフロー内で **OIDC** により取得する。長期キーを置かない。
- ローカルの S3 互換ストレージ（SeaweedFS）の認証情報はダミー値のみ。

## コミット前チェックリスト

```bash
make verify      # 層検証 + 静的解析 + テスト
make fmt         # フォーマット
```
